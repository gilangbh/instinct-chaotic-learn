# Railway deployment configuration  
# Make native modules optional - don't fail if they can't compile
# This affects hardware wallet support (Trezor/Ledger) but web wallets still work fine
legacy-peer-deps=true

